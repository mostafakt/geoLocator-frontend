import styled from "styled-components";
import { Image as AntdImage } from 'antd'

export const StyledImage = styled(AntdImage)`

.ant-image .ant-image-mask .ant-image-mask-info .anticon {
    display: block;
    margin: auto;
  }
  
`